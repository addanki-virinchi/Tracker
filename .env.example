# Example environment configuration. Copy to ".env" and fill in real values.
# Do not commit your real ".env" file.

# Flask session signing key. Generate a strong, random value, e.g.:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace-with-a-long-random-secret

# Administrator credentials. These are the source of truth for admin access
# and are applied automatically on startup.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-password

# PostgreSQL connection string (recommended).
# On Render, use the database's "Internal Database URL".
# For local development against the Render database, use the "External Database URL".
DATABASE_URL=postgresql://user:password@host:5432/database_name

# Alternatively, configure the database with discrete variables instead of
# DATABASE_URL (DATABASE_URL takes precedence if both are set):
# DB_HOST=
# DB_PORT=5432
# DB_NAME=
# DB_USER=
# DB_PASSWORD=

# Number of days a "remember me" login session stays active.
REMEMBER_DAYS=30
